# Required in production: your deployed site origin (no trailing slash). Used for metadata, sitemap, and robots.
# Example: NEXT_PUBLIC_APP_URL=https://your-domain.com
# NEXT_PUBLIC_APP_URL=

# In development and test, omitting this defaults to http://localhost:3000.
