You are helping maintain the Fund Me If You Can hackathon landing site (Next.js 16, Bun).

Before changing code:

- Read `README.md` for env vars, scripts, and structure.
- Production requires `NEXT_PUBLIC_APP_URL` (see `src/lib/env.ts` and `.env.example`).
- Prefer Bun: `bun install`, `bun run dev`, `bun run lint`, `bun run test:run`, `bun run build`.
- Do not run `npm` or `npx` unless the project owner explicitly asks (their workflow may forbid it).

Keep changes scoped to the request. After UI or copy changes, run tests and lint when possible.
